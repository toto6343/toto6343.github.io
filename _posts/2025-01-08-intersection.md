---
layout: post
title: "Intersection 프로젝트로 충돌 판별 알고리즘 구현하기"
featured-img: intersection
categories: [Algorithm, Geometry, Project]
mathjax: true
---

## 프로젝트 소개

**Intersection 프로젝트**는  
선분(Line Segment), 사각형(Rectangle) 등 **기하 도형 간의 교차 여부**를  
수학적으로 판단하는 알고리즘을 구현한 프로젝트입니다.

그래픽 처리, 게임 개발, 충돌 감지, 지도 서비스 등  
다양한 분야에서 사용되는 **기본적이지만 매우 중요한 문제**를 다룹니다.

GitHub 레포지토리:  
👉 https://github.com/toto6343/intersection

---

## 왜 Intersection 문제가 중요한가?

교차 판별 문제는 다음과 같은 상황에서 핵심 역할을 합니다.

- 게임에서 캐릭터와 장애물 충돌 감지
- 지도 서비스에서 경로 교차 판단
- 그래픽스에서 도형 겹침 처리
- 시뮬레이션에서 물체 간 상호작용 계산

이 프로젝트에서는  
**수학적 정의를 코드로 정확히 옮기는 것**에 집중했습니다.

---

## 핵심 개념: CCW (Counter Clock Wise)

선분 교차 판별의 핵심은  
**CCW 알고리즘**입니다.

세 점 \(A, B, C\)가 주어졌을 때,  
이 점들이 반시계 방향인지, 시계 방향인지, 혹은 일직선상에 있는지를 판단합니다.

### CCW 정의

$$
CCW(A, B, C) =
\begin{cases}
> 0 & \text{반시계 방향} \\
< 0 & \text{시계 방향} \\
= 0 & \text{일직선}
\end{cases}
$$

---

## 선분 교차 판별 조건

두 선분 \(AB\), \(CD\)가 교차하려면 다음 조건을 만족해야 합니다.

$$
CCW(A, B, C) \times CCW(A, B, D) \le 0
$$

$$
CCW(C, D, A) \times CCW(C, D, B) \le 0
$$

이 두 조건이 동시에 성립하면  
두 선분은 **서로 교차하거나 접촉**합니다.

---

## 코드 구현 예시

```python
def ccw(a, b, c):
    return (b.x - a.x) * (c.y - a.y) - (b.y - a.y) * (c.x - a.x)

def is_intersect(a, b, c, d):
    return (
        ccw(a, b, c) * ccw(a, b, d) <= 0 and
        ccw(c, d, a) * ccw(c, d, b) <= 0
    )
